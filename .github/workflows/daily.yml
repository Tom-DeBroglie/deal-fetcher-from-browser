name: Weekly AI Deal Web Monitor

on:
  workflow_dispatch:
    inputs:
      send_empty_report:
        description: '没有发现活动时也推送空报告（测试用）'
        required: false
        default: 'true'
  schedule:
    # 北京时间每周一 08:00 运行；UTC 周一 00:00
    - cron: '0 0 * * 1'

permissions:
  contents: write

concurrency:
  group: ai-deal-web-monitor
  cancel-in-progress: false

jobs:
  run-monitor:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      PUSHPLUS_TOKEN: ${{ secrets.PUSHPLUS_TOKEN }}
      TAVILY_API_KEY: ${{ secrets.TAVILY_API_KEY }}
      SERPER_API_KEY: ${{ secrets.SERPER_API_KEY }}
      SEND_EMPTY_REPORT: ${{ github.event.inputs.send_empty_report || 'false' }}
      MAX_SEARCH_RESULTS_PER_QUERY: '3'
      MAX_CANDIDATES_PER_RUN: '170'
      MAX_ARTICLES_TO_FETCH: '70'
      MIN_SCORE_TO_REPORT: '15'
      MIN_TOTAL_KEYWORD_HITS: '3'
      MIN_STRONG_PROMO_HITS: '1'
      MAX_FOREIGN_OFFICIAL_REPORT_ITEMS: '3'
      MAX_FOREIGN_INTEL_REPORT_ITEMS: '7'
      MAX_DOMESTIC_OFFICIAL_REPORT_ITEMS: '3'
      MAX_DOMESTIC_INTEL_REPORT_ITEMS: '7'
      REQUEST_TIMEOUT: '10'
      SLEEP_SECONDS: '0.2'
      ADD_YEAR_VARIANTS: 'false'
      ENABLE_DDG_SEARCH: 'true'
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run monitor
        run: |
          python web_deal_monitor.py

      - name: Commit updated state
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/sent_items.json data/latest_report.md || true
          if git diff --cached --quiet; then
            echo "No state changes to commit."
            exit 0
          fi
          git commit -m "chore: update monitor state [skip ci]"
          git pull --rebase --autostash origin main
          git push origin main
